{
  "buildCommand": "cd nuxt-app && npm install && npm run build",
  "outputDirectory": "nuxt-app/.vercel/output",
  "installCommand": "cd nuxt-app && npm install",
  "framework": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "https://api.logic-design-solutions.com"
        }
      ]
    }
  ]
}
